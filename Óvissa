#include<iostream>
using namespace std;

int main() {
    string inputString;
    char targetChar = 'u';
    getline(cin, inputString);
    int count = 0;
    for (char ch : inputString) {
        if (ch == targetChar) {
            count++;
        }
    }

    cout<< count << endl;

    return 0;
}
